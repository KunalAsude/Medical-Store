/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["img.icons8.com"], // ✅ Add allowed external domains
    },
  };
  
  export default nextConfig; // ✅ Use ES module syntax
  